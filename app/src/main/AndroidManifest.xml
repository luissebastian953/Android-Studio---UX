<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.software.digimonhunter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ImageCarousel"></activity>
        <activity android:name=".DigimonDetailActivity" />
        <activity android:name=".DigimonListAdapter" />
        <activity android:name=".GoldFragment" />
        <activity android:name=".SilverFragment" />
        <activity android:name=".BronzeFragment" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".DigimonActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>